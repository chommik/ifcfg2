Your IP address is <%= @ip %>

Other tools:
<%- root_url_spaces = ' ' * root_url.length -%>

<% [nil, :txt, :md, :json].each do |fmt| %>
<%= root_url %>
<%= root_url_spaces %><%= ip_path format: fmt %>
<%= root_url_spaces %><%= ip_all_path format: fmt %>
<%= root_url_spaces %><%= ip_dns_path format: fmt %>
<%= root_url_spaces %><%= http_headers_path format: fmt %>

<% end %>